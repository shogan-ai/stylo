(include dune.inc)

(rule
  (deps (source_tree .))
  (action
    (with-stdout-to dune.inc.gen
      (bash "./gen-dune.sh %{deps}"))))



(rule
 (alias  runtest)
 (action (diff dune.inc dune.inc.gen)))

