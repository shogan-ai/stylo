(library
  (name stylo)
  (libraries config ast_checker ocaml_syntax print document dbg_print
             normalize comments camtrail)
  (modules :standard \ std)
  (preprocess (pps camtrail.ppx)))

(library
  (name std)
  (modules std)
  (libraries camtrail)
  (preprocess (pps camtrail.ppx)))
