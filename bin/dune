(executable
  (name stylo)
  (libraries ast_checker ocaml_syntax print document dbg_print normalize)
  (modules :standard \ fuzzer_driver))

(executable
  (name fuzzer_driver)
  (libraries unix)
  (modules fuzzer_driver))
