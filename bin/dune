(executable
  (name stylo)
  (libraries config ast_checker ocaml_syntax print document dbg_print normalize
             comments)
  (modules :standard \ fuzzer_driver))

(executable
  (name fuzzer_driver)
  (libraries unix)
  (modules fuzzer_driver))
